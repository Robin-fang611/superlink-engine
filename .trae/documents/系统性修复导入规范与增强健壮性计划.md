## 技术实现步骤：

1. **修改 [app.py](file:///d:/business%20bot/%E9%A1%B9%E7%9B%AE/superlink%E6%95%B0%E6%8D%AE/app.py)**：
    - 将 `import asyncio` 和 `import sqlite3` 移至文件顶部。
    - 在顶部导入 `from urllib.parse import urlparse, urljoin`。
    - 统一将 `core` 模块的导入放在系统导入之后。
    - 优化 `run_enhanced_task` 的异常处理，打印 Traceback 到控制台以便深度调试。

2. **修改 [feedback_processor.py](file:///d:/business%20bot/%E9%A1%B9%E7%9B%AE/superlink%E6%95%B0%E6%8D%AE/core/feedback_processor.py)**：
    - 使用 `try...except ImportError` 包装 `from zhipuai import ZhipuAI`。
    - 在 `__init__` 中增加对 `ZhipuAI` 是否为 `None` 的检查。

3. **系统清理**：
    - 检查并移除各函数内部重复的 `import` 语句。

## 验证方案：
- 重新启动 Streamlit 服务。
- 依次测试“基础搜索”和“增强搜索（Ultra）”模式。
- 检查控制台输出，确认没有任何 `ImportError` 或 `NameError`。
- 观察 UI 上的“系统仪表盘”，确认 Serper 和智谱 AI 状态均为绿色。

请问是否按照此计划执行？如果需要，我可以立即开始修改。